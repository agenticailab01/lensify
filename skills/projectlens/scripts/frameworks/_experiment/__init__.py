"""Experiment tracking adapters (Weights & Biases, MLflow, Comet).

These adapters surface the *observability* layer: which experiments are
tracked, what metrics/params/artifacts are logged, and where runs live.
For ML teams, this answers "what runs are happening, where do I find them,
and which model versions are registered?"
"""
