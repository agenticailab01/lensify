"""Production-inference framework adapters (vLLM, Triton, BentoML, Ray Serve).

These adapters surface the *serving* surface of an AI project: model loads,
inference endpoints, deployments. For teams shipping LLMs to users, this
section answers "what's exposed, where, and which model is behind it?"
"""
