"""AI UI framework adapters (Streamlit, Gradio, Chainlit).

These are the popular Python-native frameworks for shipping LLM-backed apps
quickly. Each adapter surfaces the structural skeleton — pages, widgets,
handlers — without forcing the agent to read raw UI code.
"""
